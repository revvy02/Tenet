local Primitives = {
    ServerSignal = require(script.ServerSignal),
    ServerCallback = require(script.ServerCallback),
    ServerBroadcast = require(script.ServerBroadcast),
    ServerNetwork = require(script.ServerNetwork),
}

return Primitives