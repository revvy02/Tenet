local Primitives = {
    ClientSignal = require(script.ClientSignal),
    ClientCallback = require(script.ClientCallback),
    ClientBroadcast = require(script.ClientBroadcast),
    ClientNetwork = require(script.ClientNetwork),
}

return Primitives